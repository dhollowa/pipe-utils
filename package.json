{
  "name": "pipe-utils",
  "author": "David Holloway <davidh@slugworth.com>",
  "main": "./lib/main",
  "version": "0.10.0",
  "description": "Yet another package for running shell tools.",
  "keywords": [
    "pipe",
    "shell"
  ],
  "repository": "https://github.com/dhollowa/pipe-utils",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "etch": "^0.6.0"
  },
  "configSchema": {
    "pipeExecutable": {
      "title": "The pipe executable.",
      "description": "HACK HACK HACK TODO(dhollowa): Remove once UI is in place.",
      "type": "string",
      "default": "date"
    }
  }
}
